var sentence = prompt('Escriba una frase');

var encrypted = function (sentence) {    //transformar la variable de entrada (sentence) en un array para poder manejarlo
  var array = Array.from(sentence);
for (var i=0; i=array.length; i++){
  
}
